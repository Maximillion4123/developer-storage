# 예를 들어봅시다.
A = input()
# 이 경우 그냥 입력한 값 자체가 A에 문자열로 할당됩니다.
B = list(input())
# 이 경우 input()의 문자열이 문자 하나로 쪼개져서 리스트에 들어갑니다.
C = input().split()
# 이 경우에는 입력한 줄에 대해 공백으로 구분하여 리스트로 만들어줍니다.
for i in range(len(C)):
    C[i] = int(C[i])
# 그러면 이런식으로 C의 각 요소를 integer, 정수형으로 바꿀 수 있겠네요.
# 그런데 너무 길죠?
D = map(int, input().split())
# 이러면 이제 각 요소에 대해 int화 된 채로 map오브젝트가 반환됩니다. (오브젝트는 그냥 '상자'로 생각해주세요.)
# map오브젝트를 이제 리스트로 바꿀건데,
E = list(map(int, input().split()))
# 이러면 각 요소가 숫자가 된 채로 리스트에 담기게됩니다. 혹시 파이썬에서 *의 역할을 아신다면,
F = [*map(int, input().split())]
# 이런식으로 더 간결하게 쓰실 수도 있겠지요.